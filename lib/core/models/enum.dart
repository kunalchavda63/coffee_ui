enum FontFamily { sen }

enum ImageType { asset, network, file }

enum FabType { normal, small, large, extended }

enum AnimationTypes {
  fade,
  scale,
  fadeScale,
  slideFromTop,
  slideFromBottom,
  slideFromLeft,
  slideFromRight,
  none,
}

enum Role { user
  , admin}
